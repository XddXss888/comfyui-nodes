{
  "1": {
    "class_type": "SRL Eval",
    "inputs": {
      "parameters": "",
      "code": "\nimport subprocess\nr = subprocess.run(['nvidia-smi', '--query-gpu=name,memory.total', '--format=csv,noheader'], capture_output=True, text=True, timeout=10)\nreturn r.stdout.strip() if r.returncode == 0 else 'NO_GPU'\n"
    }
  },
  "2": {
    "class_type": "PreviewTextNode",
    "inputs": {
      "text": [
        "1",
        0
      ]
    }
  }
}