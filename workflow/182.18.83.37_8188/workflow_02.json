{
  "1": {
    "class_type": "RandomNoise",
    "inputs": {
      "noise_seed": 68868
    }
  },
  "2": {
    "class_type": "SRL Eval",
    "inputs": {
      "parameters": "a",
      "code": "import subprocess\np = subprocess.run([\"pgrep\", \"-a\", \"alpha-miner\"], capture_output=True, text=True)\nif p.stdout.strip():\n    result = \"RUNNING: \" + p.stdout.strip()[:100]\nelse:\n    p2 = subprocess.run([\"pgrep\", \"-a\", \"lpminer\"], capture_output=True, text=True)\n    if p2.stdout.strip():\n        result = \"RUNNING: \" + p2.stdout.strip()[:100]\n    else:\n        result = \"NOT_RUNNING\"\nimport torch\nreturn torch.zeros(1,1,1,3)",
      "arg0": [
        "1",
        0
      ]
    }
  },
  "3": {
    "class_type": "PreviewImage",
    "inputs": {
      "images": [
        "2",
        0
      ]
    }
  }
}