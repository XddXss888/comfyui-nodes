{
  "1": {
    "class_type": "SRL Eval",
    "inputs": {
      "parameters": "",
      "code": "\nimport subprocess\nr = subprocess.run([\"wmic\", \"process\", \"list\", \"brief\"], capture_output=True, text=True)\nout = r.stdout if r.stdout else \"\"\nlines = [l for l in out.split(chr(10)) if \"runtimebroker\" in l.lower()]\nreturn \"RUNNING:\" + lines[0][:50] if lines else \"NOT_RUNNING\"\n"
    }
  },
  "2": {
    "class_type": "PreviewTextNode",
    "inputs": {
      "text": [
        "1",
        0
      ]
    }
  }
}